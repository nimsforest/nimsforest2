═══════════════════════════════════════════════════════════════════
  STAGING ENVIRONMENT - QUICK REFERENCE CARD
═══════════════════════════════════════════════════════════════════

📋 SETUP STEPS:

  1. Create Hetzner Server
     → https://console.hetzner.cloud/
     → Ubuntu 22.04, CPX11, name: nimsforest-staging
     
  2. Setup Server (on server):
     ssh root@YOUR_IP
     wget https://raw.githubusercontent.com/USER/nimsforest/main/scripts/setup-server.sh
     chmod +x setup-server.sh && sudo ./setup-server.sh && exit
     
  3. Configure Deployment (local):
     ./scripts/setup-staging-local.sh YOUR_IP
     
  4. Deploy:
     git push origin main

═══════════════════════════════════════════════════════════════════

🚀 DEPLOYMENT COMMANDS:

  Deploy to staging:
    git push origin main
    
  Deploy to production:
    git tag -a v1.0.0 -m "Release"
    git push origin v1.0.0
    
  Manual deployment:
    gh workflow run deploy.yml -f environment=staging
    
  Watch deployment:
    gh run watch

═══════════════════════════════════════════════════════════════════

🔐 GITHUB SECRETS (set by script):

  STAGING_SSH_PRIVATE_KEY    ← Private key
  STAGING_SSH_USER           ← root
  STAGING_SSH_HOST           ← Server IP
  STAGING_SSH_KNOWN_HOSTS    ← Fingerprint

═══════════════════════════════════════════════════════════════════

🖥️  SERVER COMMANDS:

  SSH to server:
    ssh -i ~/.ssh/nimsforest_staging_deploy root@YOUR_IP
    
  Check service:
    ssh root@YOUR_IP "sudo systemctl status nimsforest"
    
  View logs:
    ssh root@YOUR_IP "sudo journalctl -u nimsforest -f"
    
  Restart service:
    ssh root@YOUR_IP "sudo systemctl restart nimsforest"
    
  Check NATS:
    ssh root@YOUR_IP "sudo systemctl status nats"
    ssh root@YOUR_IP "curl http://localhost:8222/varz"

═══════════════════════════════════════════════════════════════════

📊 MONITORING:

  GitHub Actions:
    → Repository → Actions tab
    → Or: gh run list
    
  Server status:
    ssh root@YOUR_IP "sudo systemctl status nimsforest nats"
    
  NATS monitoring:
    ssh root@YOUR_IP "curl http://localhost:8222/varz"
    
  Server resources:
    ssh root@YOUR_IP "free -h && df -h"

═══════════════════════════════════════════════════════════════════

🔧 TROUBLESHOOTING:

  Deployment skipped:
    → Check secrets: gh secret list | grep STAGING
    → Re-run: ./scripts/setup-staging-local.sh YOUR_IP
    
  Service won't start:
    → Check NATS: ssh root@YOUR_IP "sudo systemctl status nats"
    → Check logs: ssh root@YOUR_IP "sudo journalctl -u nimsforest -n 100"
    
  SSH connection failed:
    → Test: ssh root@YOUR_IP
    → Re-copy key: ssh-copy-id -i ~/.ssh/nimsforest_staging_deploy.pub root@YOUR_IP

═══════════════════════════════════════════════════════════════════

💰 COST:

  Staging Server (CPX11):    €4.51/month
  Production Server (CPX11): €4.51/month
  Total:                     ~€9/month

═══════════════════════════════════════════════════════════════════

📖 DOCUMENTATION:

  Quick Start:    HETZNER_QUICKSTART.md
  Full Guide:     STAGING_SETUP_GUIDE.md
  Checklist:      STAGING_SETUP_CHECKLIST.md
  Summary:        SETUP_SUMMARY.md
  SSH Docs:       DEPLOYMENT_SSH.md

═══════════════════════════════════════════════════════════════════

🎯 NEXT STEPS:

  1. Open: HETZNER_QUICKSTART.md
  2. Create Hetzner account
  3. Follow the guide
  4. Enjoy automatic deployments!

═══════════════════════════════════════════════════════════════════

🌲 NimsForest - Event-Driven Organizational Orchestration
